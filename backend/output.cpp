#include <iostream>
#include <string>
#include <cmath>
using namespace std;

void is_odd_or_even(int number) {
    if (((number % 2) == 0)) {
        return Even;
    }
    else {
        return Odd;
    }
}